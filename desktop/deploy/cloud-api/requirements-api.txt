# Headless cloud API dependencies (no PyQt / desktop libs).
# Reads the SAME timber.core against Supabase Postgres.
fastapi==0.139.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32       # multipart/form-data for /backup/restore-upload
pyjwt==2.13.0
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
bcrypt==4.2.1
python-dotenv==1.0.1
# report export (server-side PDF / Excel via /reports/export)
reportlab==4.2.5
openpyxl==3.1.5
arabic-reshaper==3.0.0       # Urdu shaping in PDFs
python-bidi==0.6.3           # RTL bidi for PDFs
