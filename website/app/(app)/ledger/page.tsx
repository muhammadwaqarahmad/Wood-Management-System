"use client";

import { PartyStatement } from "@/components/PartyStatement";

/* Supplier Ledger — desktop PartyLedgerScreen (PARTY_BAPARI). */
export default function SupplierLedgerPage() {
  return <PartyStatement kind="supplier" />;
}
